/**
 * Bud file for index.js
 */

"use strict";

var indexJsBud = require('coz/lib/bud/factory/index_js_bud'),
    pkg = require('../package.json');

module.exports = indexJsBud({
    desc: pkg.description,
    module: pkg.name,
    dirname: __dirname
});

if (!module.parent) {
    require('coz').render(__filename);
}
